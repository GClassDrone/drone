<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gclass.drone.mapper.BoardMapper">

<!-- 가입시 받는 값 -->

	<insert id="create">
		insert into board ( subjno, bno, mno, ttl, ctt, readcnt, regdt)
		values(#{subjno}, #{bno}, #{mno}, #{ttl}, #{ctt}, #{readcnt}, #{regdt})
	</insert>

<!--프로필 상세 페이지볼 때 읽어오는 값들 -->

	<select id="read" resultType="BoardDto">
		select bno, mno, ttl, ctt, readcnt, regdt
		from board
		where bno= #{bno}
	</select>


<!-- 글 수정할때 수정가능한 값들 -->
	<update id="update"> <!-- 수정해야됨 -->
		update board set ctt = #{ctt}
		where bno = #{bno}
	</update>

<!-- 삭제시에 삭제는 글번호로 -->
	<delete id="delete">
		delete from board where bno = #{bno}
	</delete>
</mapper>